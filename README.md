# POKER-DATA-EXTRACTION

There is three extraction algorithms : one for extracting the MLP inputs, one for extracting the RNN + Transformer inputs and one for extracting the Transformer inputs.

## The dataset


## 
